# Generate a key with: openssl rand -hex 32
KANBAN_API_KEY=

# Optional — override the host port (default: 80)
# PORT=80

# Optional — default agent name shown on cards created by the MCP server
# KANBAN_AGENT=claude
